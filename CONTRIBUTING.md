# Contribuer

Créez une branche `feat-*`, `fix-*`, `test-*` ou `docs-*`. Avant une pull request, exécutez `npm run format:check`, `npm run lint`, `npm run typecheck`, `npm test` et `npm run build`. Les commits doivent rester ciblés et décrire le comportement changé.
