# Développement

Node.js 20+ et npm 10+ sont recommandés. Installez les dépendances avec `npm install`. Les commandes disponibles sont `npm run dev`, `npm run build`, `npm run lint`, `npm run format`, `npm run typecheck` et `npm test`.
